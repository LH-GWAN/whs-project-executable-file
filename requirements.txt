# IDAS dependencies
#
# The analysis engine (engine/vendor/) and backend logic use only the
# standard library. The packages below are for the GUI and packaging.
# Korean documentation: see README.md and BUILD.md

PySide6>=6.7          # QtWidgets + QtWebEngine (PDF report) + QtMultimedia (video)
pyinstaller>=6.10     # exe packaging (build time only)
